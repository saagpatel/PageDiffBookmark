# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state. No code was changed and no builds or tests were run. It exists so a reviewer can audit what changed and why.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`  
**Evidence:** `README.md`, `manifest.json:1–36`, `background/service-worker.js`

The README and CLAUDE.md accurately describe PageDiffBookmark as a Chrome MV3 extension that tracks arbitrary web pages for content changes, diffs them with diff-match-patch + Mozilla Readability, and surfaces results via OS notifications, badge count, and a side panel. All stated capabilities (context menu, popup button, auth-wall detection, no external servers) are confirmed in the code.

---

### 2. Current state

**Status:** `drifted` — one issue added to README Known Issues  
**Evidence:** `background/service-worker.js:7`, `IMPLEMENTATION-ROADMAP.md:415–416`

All three planned phases (Phase 0 scaffold/bookmarks, Phase 1 polling/diff/notifications, Phase 2 diff viewer/controls/settings) are fully implemented. The README's "Status: Work in Progress — Cross-browser support not yet implemented" accurately describes the shipping gate. However, `DEV_POLL_INTERVAL_MINUTES` is hardcoded to `1` at line 7 of `service-worker.js`, overriding all per-bookmark configured intervals (1h/3h/6h/12h/24h) and making every alarm fire every minute. The README's Known Issues section did not mention this. `IMPLEMENTATION-ROADMAP.md` (lines 415–416) identifies it as a dev shortcut that must be set to `0` before shipping, but README had no such warning.

**Change:** `README.md` — added a third bullet to Known Issues:  
_Before:_ two bullets (service worker termination, SPA extraction)  
_After:_ added `DEV_POLL_INTERVAL_MINUTES` override warning

---

### 3. Stack

**Status:** `consistent`  
**Evidence:** `manifest.json:2`, `background/service-worker.js:3–4`, `lib/diff-match-patch.js`, `lib/Readability.js`, `popup/popup.html`

All stack claims verified: Chrome MV3 (`manifest_version: 3`), service worker (`background.service_worker`), Readability.js and diff-match-patch both bundled under `lib/`, `chrome.storage.local` throughout, vanilla HTML/CSS/JS with no build step and no framework.

---

### 4. How to run

**Status:** `consistent`  
**Evidence:** absence of `package.json` in repo root; no bundler config; `manifest.json` loadable directly

"No build step required — load unpacked in Chrome" is accurate. The extension loads directly from the project root with no compilation needed. (See also *Contradictions for Manual Review* below regarding the Makefile.)

---

### 5. Known risks

**Status:** `drifted` — see Current State above  
**Evidence:** `background/service-worker.js:7` (dev override), `service-worker.js:262–264` (500ms SPA delay), `service-worker.js:516–527` (auth-wall detection)

The two existing Known Issues (service worker termination / SPA extraction) are verified accurate. The `DEV_POLL_INTERVAL_MINUTES = 1` override is the additional risk now documented in README.

---

### 6. Next move

**Status:** `consistent`  
**Evidence:** `IMPLEMENTATION-ROADMAP.md` (Deferred to v2 section), `README.md` status note

The README accurately states cross-browser support is the outstanding gap. IMPLEMENTATION-ROADMAP.md itemizes v2 deferred features (diff history, selector scoping, export, bulk operations). No stubs or TODOs exist in the source that contradict the "phases complete" framing; `content/content-script.js` is intentionally a placeholder (see CLAUDE.md fix below).

### 6a. Architecture description (CLAUDE.md)

**Status:** `drifted` — CLAUDE.md content/ description updated  
**Evidence:** `content/content-script.js:1–5` (placeholder comment), `background/service-worker.js:238–255` (injectAndExtract uses `executeScript({ func })`)

CLAUDE.md described `content/` as "content script: page text extraction via Readability before sending to background." In practice, `content-script.js` is a stub that holds only a comment; all extraction happens via a function (`extractPageContent`) injected at runtime by the service worker using `chrome.scripting.executeScript({ func })`. When polling without an active tab, the service worker uses `DOMParser + Readability` entirely in-process.

**Change:** `CLAUDE.md:28` — updated content/ description  
_Before:_ `content script: page text extraction via Readability before sending to background`  
_After:_ `content script placeholder; extraction is injected at runtime via chrome.scripting.executeScript({ func }) from the service worker (content-script.js is a stub)`

---

## Contradictions for Manual Review

These are in files this pass is not permitted to edit.

### `Makefile` — npm scripts with no package.json

The `Makefile` defines `install`, `dev`, `build`, `test`, `lint`, and `clean` targets that delegate to `npm install` / `npm run <script>`. There is no `package.json` in the repository, so all of these targets will fail with `npm error Tracker "idealTree" already exists`. The Makefile is an OSS scaffold artifact (added in a `chore: add Makefile` commit) that does not reflect the actual build model of this project (no build step, no npm). A human should either add a `package.json` with appropriate scripts or replace the Makefile targets with Chrome-extension-appropriate equivalents (e.g., a `lint` target using `eslint` invoked directly).

### `docs/PORTFOLIO-DISPOSITION.md` — stale `origin/main` tip SHA

The document records `origin/main` tip as `50447d4` in both the "Verification posture" section (line 29) and the "Last known reference" table (line 231). The current HEAD is `18bbea28` (fix: clear high CodeQL alerts), four commits later. Subsequent commits are `docs(context)`, `ci:`, and `fix:` (CodeQL security), none of which invalidate the disposition analysis. A human should update both references to `18bbea28` and note the four post-audit commits.

---

## Footer

Generated: **2026-05-30 21:05:57 PDT**  
Branch: `docs/truth-up-2026-05-30`  
HEAD reconciled against: `18bbea28ee668d302cfcb2f82f1febf021f3b036`
